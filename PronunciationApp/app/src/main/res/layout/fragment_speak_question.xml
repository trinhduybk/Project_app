<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/activity_horizontal_margin">
    <LinearLayout
        android:id="@+id/layout_request_speaking_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/tv_request_speaking_question"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginBottom="@dimen/activity_horizontal_margin"
            android:gravity="center_vertical"
            android:text="@string/speaking_practice_sound"/>

    </LinearLayout>

        <TextView
            android:id="@+id/tv_word_practice_speaking_question"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/star_practice_size"
            android:text="book"
            android:layout_below="@id/layout_request_speaking_question"
            android:textSize="@dimen/inputword_text_size"
            />

    <ImageView
        android:id="@+id/btRecord_speaking_question"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/record"
        android:layout_below="@id/img_result"
        android:layout_marginTop="@dimen/margin_top_recorder"
        android:layout_centerInParent="true"
        />
    <Button
        android:id="@+id/bt_next_question_speaking_question"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@drawable/button"
        android:layout_marginBottom="@dimen/practice_listening_button_check_margin"
        android:layout_marginLeft="@dimen/practice_listening_button_check_margin"
        android:layout_marginRight="@dimen/practice_listening_button_check_margin"
        android:text="@string/next_question"/>

</RelativeLayout>